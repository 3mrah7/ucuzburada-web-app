# CheapHere E-Commerce Project

This is a full-stack e-commerce application developed as a final project.

## 🚀 Features
* **Real-time Search Bar:** Filter products instantly.
* **Dynamic Listing:** Products are fetched from a PostgreSQL database via Spring Boot.
* **Responsive UI:** Built with React and Material UI.

## 🛠 Tech Stack
* **Frontend:** React, Material UI, Axios
* **Backend:** Java, Spring Boot, Maven
* **Database:** PostgreSQL

## ⚙️ How to Run
1. **Clone the repository** to your local machine.
2. **Frontend:** Go to the `frontend` folder, run `npm install` to install dependencies (node_modules), then run `npm start`.
3. **Backend:** Run the Spring Boot application using Maven or your IDE.
4. **Database:** Make sure your PostgreSQL is active and configured.
